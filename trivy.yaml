severity:
  - CRITICAL
  - HIGH

# Fail build on any CRITICAL CVE
exit-code: 1

# Output format
format: table

# Skip dev/test dependencies (only scan runtime)
skip-dirs:
  - .git
  - target/test-classes

# Ignore specific CVEs (only after security team approval + JIRA ticket)
# ignorefile: .trivyignore
